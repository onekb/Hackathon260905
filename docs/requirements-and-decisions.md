# 需求与决策记录

> 地址脱敏说明：`demo.example.com` 为占位域名，不是实际部署或验收地址；本文历史验证记录指向清理前的真实地址。

最后整理：2026-09-05，Asia/Shanghai。实现程度见 [进度](progress.md)，接口定义见 [架构与协议](architecture-and-protocol.md)。

## 目标与范围

让可合法提供的闲置推理能力与按需买家连接起来，目标是减少闲置并让买家获得更低成本的选择；当前尚无真实价格优势或资源利用率验证。买家从独立卖家购买一次请求，并看清报价、预算、用量、费用和链上结算结果。黑客松优先展示 Web3 能直接约束的资金规则；用户在需求讨论开始时表示约有 8 小时开发窗口，这不是当前剩余时间。

| 范围 | 已采用要求 |
| --- | --- |
| 网络 | Monad 测试网，chain ID `10143` |
| 买家 | 在线体验台和最小 API 共用一个订单及结算流程 |
| 卖家 | 自行运行独立进程及本地控制台；模拟响应，不接真实模型或算力机房 |
| 连接 | 买家访问平台 API；卖家主动通过 WSS 接入 Router，无需公网地址 |
| 报价 | 普通输入、缓存读取、缓存写入、输出分别报价，按百万模拟 Token 计价 |
| 预算 | 每次请求设置上限；卖家最低要求采用最低预留金额，不是最低消费 |
| 失败与取消 | 卖家故障整单推理费为零；买家显式取消收已计量实际费用；剩余预算释放 |
| 资金 | 原生测试 MON 托管、受限消费授权、锁款、结算、超时自行回收、提款；D17 剥离旧 dUSD 产品代码 |
| 内容与信任 | 内容不上链；平台和卖家可见请求；平台计量和判责，合约验证资金规则 |
| 仓库 | `onekb/Hackathon260905`，使用 SSH；公开发布范围按用户实际要求执行 |
| 比赛交付 | 官方要求公开提交材料、应用部署 Monad、前端公网部署，应用与前端长期可用；MOJO 组队和提交，规则与未决项见 [比赛材料](hackathon-submission.md#已核实的比赛要求) |
| 参赛队伍 | 用户明确独立参赛，队名 InferPool；MOJO 已建立仅邀请队伍、1/3，用户为队长；项目 #385 已提交、待审核 |

首版不包含真实模型验证、TEE、投诉裁决、扣质押、完整订单簿、批量结算、桌面安装包、多模态和工具调用，也不承诺兼容所有 OpenAI SDK 行为。2026-09-05 后续读取官方页面后，已确认公开材料、长期公网部署、每队五分钟实机演示与五项必需提交材料；截止计划标为 18:30，日期/时区及对应场次仍按现场确认。视频后备是建议、Slides 可选，不能沿用早期“尚无已知公网要求”的状态，详细来源见比赛材料。

## 路演定位澄清（2026-09-05）

用户将三页主线调整为 **供需价值 → Web3 与 Monad 双向价值 → 使用流程**。供给是闲置推理能力或可合法提供的服务；Token 是计量单位，不是可闲置库存，现版不声称转售第三方任意额度。“更便宜、减少闲置”是商业目标，尚无真实价格或利用率数据支撑。

Monad 提供原生 MON 结算、预算托管、可追溯资金记录和 EVM 钱包工具；InferPool 计划反向为 Monad 应用与 Agent 提供统一推理 API、带来按需推理支付场景。前者已有本项目测试网链路证据，后者尚无生态合作落地。此次澄清不新增真实模型、供给接入或合作完成声明；AI/计量仍 Mock，Router 仍受信计量与判责。

## 验收要求

1. 两个独立卖家拥有不同钱包与报价；买家能指定或自动匹配卖家。
2. 网页和 API 发起请求，显示相同口径的账单与 Monad 测试网交易。
3. 已输出部分内容后卖家失败，整单推理费为零。
4. 显式取消只收取消生效前实际用量；例如预留 `1.00`、实际 `0.20`，释放 `0.80`。
5. 预算或输出上限触顶正常终止，费用不超过预算。
6. 缓存分类不重复计算输入，不跨买家、卖家或模型共享。
7. 幂等重试不重复锁款；并发请求不超出余额或授权。
8. 平台失联后，买家可在链上期限到达后自行回收并提款。
9. 全部界面和说明明确标识模拟推理、模拟计量与测试资产。

以上是验收目标，不表示全部已通过。分环境证据见 [进度](progress.md)。

## 决策记录

以下编号稳定保留。前序事件缺少精确时间，统一按 2026-09-05 的讨论顺序记录；不将缺失原题的编号回答还原成虚构问题。

### D01 — 黑客松保留独立节点，推理使用 Mock

- **背景：** 用户要求短时间交付，同时明确不考虑算力机房和客户端上游接入。
- **选择：** 卖家独立运行软件，提供可重复的模拟响应与故障模式；真实资金流程放在测试链上。
- **原因：** 可验证网络连接、预算与结算，不把真实模型接入放在关键路径。
- **后果：** Token、缓存和模型输出不能作为真实推理能力证明，界面必须标明 Mock。
- **依据：** 用户“输出的 AI 响应是 mock 的数据”“现在先不考虑”；[MVP 规格](../MVP_SPEC.md)。

### D02 — 网页与最小 API 同时提供

- **背景：** 用户尚未确定买家使用方式，随后选择同时有在线界面与最小 API。
- **选择：** 网页体验台、API Key 和文本聊天/SSE API 共用 Router。
- **原因：** 兼顾现场演示与开发者接入，避免两套计费逻辑。
- **后果：** API 只承诺已实现的文本聊天子集；网页仍需单独进行钱包及浏览器验收。
- **依据：** 用户“1.C 既要有在线 也要有最小 AP”；[MVP 规格](../MVP_SPEC.md)。

### D03 — 卖家主动长连接到中心 Router

- **背景：** 用户询问买家如何接卖家的 API，是否需要同步公网地址；接受平台承担链外处理。
- **选择：** 买家 HTTPS/SSE → Router；卖家主动 WSS → Router；钱包签名认证身份。
- **原因：** 卖家不需公网 IP、端口映射或部署公开 API，Router 能统一控制派单及预算。
- **后果：** 平台成为可见内容且受信的中介；生产入口需支持长连接。断线不重放订单。
- **依据：** 用户连接方式提问；[MVP 规格“连接与派单”](../MVP_SPEC.md)；[provider-hub.ts](../server/src/provider-hub.ts)。

### D04 — 按实际用量收费，四项报价与最低预留

- **背景：** 用户希望输入/输出分开报价，考虑缓存，单次预算由卖家设置最低要求；最初提出买家责任“100% 扣费”。
- **选择：** 四项报价；最低要求解释为最低预留；买家显式取消扣已产生的实际费用，剩余释放；卖家责任整单为零。
- **原因：** “买家责任”需要明确收费基数，不能自动等同于收满未消费预算。此解释已进入采用的 MVP 规格。
- **后果：** 取消前的输入和缓存用量也计费；网络断开不等同于显式取消；预算触顶属于正常结束。
- **依据：** 用户报价及责任要求、后续采用建议的回答；精确定义以 [MVP 规格“责任与终态”](../MVP_SPEC.md) 为准，不补造缺失的逐轮确认原文。

### D05 — 合约控制资金，Router 判定链外事实

- **背景：** 模型真实性和故障原因不能仅靠结算合约自动验证。
- **选择：** 合约保存报价快照、消费授权、预算和终态；Router 负责模拟用量和责任结果。每单设截止时间，买家到期可自行回收。
- **原因：** 把可以验证的资金约束放到链上，明确首版仍有哪些信任假设。
- **后果：** 不得宣传为无需信任的推理验证；恶意 Router 仍可能在授权范围内提交不真实的用量或责任。授权不包含提款权。
- **依据：** [MVP 资金边界](../MVP_SPEC.md)、[InferenceMarket.sol](../contracts/src/InferenceMarket.sol)。

### D06 — TEE、投诉和扣质押延后

- **背景：** 用户提出第二版可部署 TEE 或由另一功能裁量假模型并扣质押。
- **选择：** 保留为后续方向，首版不实现。
- **原因：** TEE 本身不等于证明上游模型身份；罚没需要请求绑定、可靠证据、裁决与申诉规则。
- **后果：** 回答风格和模型评分不能直接作为自动扣款依据；首版没有押金或罚没能力。
- **依据：** 用户的第二版建议、[MVP 第二版方向](../MVP_SPEC.md)。

### D07 — Monad 测试网与自建 DemoUSD

- **背景：** 用户确认必须部署 Monad 测试网，并希望 Web3 部分有助于比赛展示。
- **选择：** chain ID `10143`，自建六位精度 DemoUSD；展示报价、锁款、结算及超时回收。
- **原因：** 清楚区分测试资产和真实美元，避免把自建币称为官方 USDC。
- **后果：** Gas 使用测试 MON；失败免除的是推理费，已发生的 Gas 不会回退；必须保留真实交易证据。
- **依据：** 用户比赛要求回复；[部署记录](../contracts/deployments/inferpool-monad-testnet.json)。

### D08 — 使用 SSH 连接既有 GitHub 仓库

- **背景：** 用户提供 `onekb/Hackathon260905` 并要求“你用 ssh 的方式接吧”。
- **选择：** 初始化本地 `main`，设置 `git@github.com:onekb/Hackathon260905.git` 作为 origin。
- **原因：** 沿用用户指定仓库和现有 SSH 认证。
- **后果：** 成功连接不等于代码已提交或推送；公开发布状态单独记录。
- **依据：** 用户仓库/SSH 消息；本次检查的 `git remote -v` 与 `git status`。

### D09 — 钱包授权由用户完成，后台常规配置由 agent 执行

- **背景：** 测试网签名需要 Alchemy session，买家钱包需要 Para；用户完成登录后不熟悉组织创建操作。
- **选择：** Router 使用已授权 Alchemy session；用户明确授权 agent 代操作 Para 后，创建 InferPool FREE 组织与项目、配置前端公开 Key。
- **原因：** 复用已授权身份，避免收集私钥；让用户只处理必须由本人完成的认证步骤。
- **后果：** 登录或配置完成不代表买家钱包交易验收通过；签名会话过期需重新认证。Para 私密 Key 不进入项目文档。
- **依据：** 用户“我已授权”、Para 登录完成回复、“你能帮我操作吗”；当前任务的后台操作结果。前端 SDK 方案见 D11，验证状态见进度。

### D10 — 文档随开发和后续对话持续更新

- **背景：** 用户担心后续无法了解完整项目始末。
- **选择：** 专职文档 agent 维护 `docs/`，根 `AGENTS.md` 约束每轮实质变化同步主题文档、进度与对话日志。
- **原因：** 将决定、实现、证据和未决事项分开保存，方便后续接手。
- **后果：** 主 agent 每次里程碑需主动同步，不能仅依赖子 agent 记忆；不创建定时任务，不记录凭证或原样复制聊天。
- **依据：** 用户“要一边写，一边完善文档……包括后面的对话，也是要同步完善文档的”。

### D11 — Para Lite 邮箱内嵌钱包与真实本地签名器

- **背景：** 用户没有要求外部钱包；full SDK 引入与当前 EVM 场景无关的多链依赖，首次审计有待处理项。
- **选择：** 使用 Para 3.18 Lite、`react-core/evm/viem` 与 `viem-v2-integration` 的邮箱内嵌 EVM 路径，暂不加载外部钱包连接器或 WalletConnect。
- **原因：** 减少依赖和新增注册项；保留完成本次钱包认证与测试网资金操作所需能力。
- **后果：** 首版入口为邮箱内嵌钱包。交易须保留 Para 提供的真实 `LocalAccount`，不能用地址字符串替换。轻量路径要重新完成审计和交互验证；CLI doctor 对该路径的静态误报不能当作已验收或阻止独立检查的依据。
- **依据：** 当前实现决策、[para-wallet.tsx](../web/components/para-wallet.tsx) 与本机 CLI/SDK 源码检查；最终结果见 [进度](progress.md)。

### D12 — 第二卖家先隔离会话，再核对钱包身份

- **背景：** Router 与已有卖家使用同一 Alchemy session；直接在原 CLI 配置重连会撤销旧 session，影响正在使用的签名能力。
- **选择：** 保留 Router 当前会话；如以后增加第二卖家，只给其进程使用位于不同父目录的 `ALCHEMY_CONFIG` 配置文件路径，由用户在 Dashboard 批准目标钱包，再读取不同地址验证。当前先推进 Para 独立买家流程。
- **原因：** `--instance-name` 只是标签；CLI 无钱包 ID 选择器，配置目录独立也不能证明第二 EOA。身份必须以实际地址和签名认证为准。
- **后果：** 不使用原配置 `--force` 来“增加卖家”；没有新钱包或第二卖家完成声明。买家地址不同于卖家只证明买卖身份分离，不构成双卖家市场验收。
- **依据：** 本轮对 Alchemy CLI 0.24.0 的只读源码检查与 [官方 CLI 说明](https://www.alchemy.com/docs/alchemy-cli#wallets-and-signing)；具体函数和文件位置见 [运行手册](runbook.md#增加第二卖家时保留-router-会话)。没有执行会话切换或新增交易。
- **后续路径：** 用户授权继续后，实际 Dashboard 仅显示既有一个 EVM 钱包，当前页面未发现新增/切换入口。D13 改用现有 Para 钱包作为第二卖家的浏览器签名身份；本条保护 Router 会话及按实际地址验收的原则继续适用，独立 Alchemy 配置不再作为本轮执行路径。

### D13 — 浏览器钱包为独立 Provider 签署认证挑战

- **背景：** 用户已授权推进第二卖家。当前 Alchemy Dashboard 未提供可见的新钱包/切换入口，而隔离 CLI 配置只能隔离会话存储，不能保证第二 EOA；现有 Para 钱包已实际可用且地址不同于卖家 A。
- **选择：** 正在实现第四种互斥 Provider 身份：`--browser-wallet <address>` 与 `--wallet-ui <origin>`。本地控制台通过严格校验 origin 与 source 的 `postMessage`，让 Web `/provider-connect` 使用 Para 签署一条受限的 Provider 认证挑战，再交由节点完成 Router 认证；不导出私钥，不新增 Alchemy 会话，不增加交易权限。本地 HTTP 保留 CSRF/同源约束，不增加 CORS。
- **原因：** 将第二卖家接入做成可复用的产品能力，让现有邮箱钱包控制独立节点，同时保留 Router 原有签名会话。
- **后果：** Para 地址 `0xbc81…4a75` 可兼任买家和卖家 B，Alchemy 地址 `0xac80…4a08` 继续作为卖家 A / Router，并作为本轮测试买家。A 买 B 是跨钱包成交；自动匹配到 A 时买卖角色重合，不能宣传成第三个独立钱包。浏览器签署的节点认证不等于链上发布报价或授予交易权限，报价仍需独立交易与回执。
- **报价与验证计划：** A 的普通输入/缓存读/缓存写/输出为 `30 / 3 / 37.5 / 80`，B 拟为 `60 / 6 / 75 / 40` dUSD / 百万模拟单位，最低预留均为 `0.0001 dUSD`。计划检查指定 B、短输入/大输出上限自动选 B、长输入/小输出上限自动选 A，并核对各自结算。
- **状态与依据：** 用户“你尝试下吧”、主 agent 实际 Dashboard 观察及本轮实现分工。Provider 核心/控制台、Web 接入页与 smoke 已写入并通过自动检查，B 报价、真实认证/下线/重连均有证据；逐笔成交结果与异常集中记录在 [进度](progress.md)，不将其中一个场景成功当作全部验收。买家钱包切换仍未实际验证。

### D14 — 公网演示使用持久新单限额与明确代理信任

> 次数、额外并发及暂停策略已由 D19 的用户明确要求取代；以下保留工程保护的引入过程。`ROUTER_TRUST_PROXY` 的代理边界未被取消。

- **背景：** 用户授权准备公网 Demo，官方又要求长期可访问；开放推理会消耗 Router 的测试 MON，CORS 和 API Key 不能单独限制所有新订单。
- **选择：** 新增显式启用的限额：每钱包未结并发 1、每 UTC 日 6 次，全局未结并发 2、本场 10 次；以固定起点和原账本统计尝试，失败锁款也计次。暂停新单保留读账、准确幂等重放、取消及结算/恢复。代理信任仅支持 none/loopback。
- **原因：** 约束演示接单规模，防止换 Key 或常规重启刷新额度；不因限额阻断已受理订单的资金处理。固定场次次数不是 Gas 的精确价格保证。
- **后果：** 必须保留绝对账本路径和固定起点，未结订单跨起点仍占并发；人工暂停通过受控重启。公网反代覆盖转发地址、Router 只在本机可达；多实例和无持久账本部署不适合当前存储实现。
- **状态与依据：** [runtime-config.ts](../server/src/runtime-config.ts)、[Router README](../server/README.md) 和新增专项测试已通过；未配置时默认关闭；现已完成远端授权及原账本迁移，带固定 epoch 的远端 Router 已运行。公网页面与 WSS 通过，实际限额拒单及流式推理仍待验收。

### D15 — 应用准备单个 HTTP 端口，用户负责 HTTPS 反向代理

- **背景：** 用户提供现成服务器、选择域名 `demo.example.com`，随后明确要求部署应用并准备端口，由用户自己配置反向代理 HTTPS。
- **选择：** agent 准备独立 Node 运行环境、应用目录、持久账本和远端 Alchemy 授权；新增可选静态目录，使 Web、API、SSE 与 WebSocket 共用一个 HTTP 入口。用户负责既有代理上的域名、TLS 与 HTTPS 配置，不由 agent 修改 TLS/1Panel。
- **原因：** 按用户明确分工交付一个应用入口，同时保护服务器上既有站点和工作负载。
- **后果：** 目标浏览器/API origin 为 `https://demo.example.com`，构建、认证来源和卖家重连须一致；HTTP 入口确定为 `127.0.0.1:8788`，同机 host 网络 OpenResty 可访问。Provider A 必须通过 `wss://demo.example.com/provider` 匹配签名域，需等待用户 HTTPS 可用后上线。HTTP、HTTPS、钱包/链上交易仍分别验收。
- **状态与依据：** 用户原话“你部署上去，准备好端口就可以了，我会部署反向代理的 HTTPS 的”。单端口 `319c6b9` 已推送并放入远端 release，82/82 和审阅通过；Node、Alchemy CLI、两份 unit/env、Linux verify 和设备登录已完成。用户已配置全站代理，agent 仅读审查、未修改代理或证书。首次钱包申请超时后已重申请并获批准；原账本安全迁移、旧 Router 停止后，Linux Router 已 enable/start。早期 502 已解决，回环及公网 /health 200；随后公网页面、配置及常驻卖家 A 的真实 WSS 认证通过，按用户最后分工完成应用/端口交付；新域名钱包登录、SSE 推理与结算仍待后续验收，B 尚未公网常驻。

### D16 — 用户批准迁移原生测试 MON，保留旧 dUSD 资金与历史

**后续替代：** D17 按用户新要求取消产品中的旧资产 UI、兼容读取和凭证迁移；本条保留先前取舍过程，现行方案以 D17 为准。

- **背景：** 用户先询问是否可将 dUSD 改为 MON 收付，完成只读可行性评估后明确回复“可以的，修改吧”，并允许自行操作水龙头。迁移为统一测试网演示体验，不是已读赛事规则强制要求。
- **选择：** 实施独立原生 MON 市场：买家存入 MON 托管，对新市场重新签署额度/期限授权，逐单锁款、结算为卖家内部可提款收益。去除 ERC-20 approve，保留 Router 受限消费授权和钱包 Gas 余额；先验证新版，再切换公开入口。用户同时允许为原 Router 钱包领取 MOJO 100 MON 活动水龙头；后续已领取并独立核对回执，见 [迁移进度](progress.md#原生-mon-迁移水龙头与本地验证)。
- **原因：** 统一买家测试资产上手路径；现有 ERC-20 市场没有升级入口，固定 token/router 和 6 位小数，不能只改名称或 token 地址来支持原生资产。
- **后果：** 新合约、新账本和资金授权按 market/资产隔离；旧订单、恢复和提款入口保留，旧 dUSD 不兑换 MON，旧 5 dUSD 授权不适用于新市场。资产精度改为 18 位，每百万计费分母仍为 1e6，二者必须拆开；旧预算、存款、报价不得当成等值 MON 直接重标。原生提款需保护资金守恒及重入边界。
- **实施默认提案：** 新版存款 0.1 MON、Router 授权 0.05 MON / 24 小时、单请求预算 0.001 MON；卖家 A 输入/缓存读/缓存写/输出报价 0.3/0.03/0.375/0.8 MON 每百万模拟单位。这些是待实现和验证的新默认值，不是 dUSD 兑换率或已发生的存款/授权。
- **状态与依据：** 用户上述明确授权。根负责后端、部署与水龙头，Router agent 负责合约/金额处理，Provider agent 负责前端，文档随真实里程碑更新。100 MON 水龙头已到账；原生市场已部署并验证 2/2、根 91/91 与 Web 静态检查通过。同钱包小额实链 8 笔交易成功（正常 .000110 MON、卖家失败 0、提款 .001），公网尚未切换；旧线上 dUSD 版继续作为当前已验证系统。旧 Key 跨资产 POST 409 并返回原请求 ID，避免重试误扣 MON。

### D17 — 正式产品仅 MON，旧 dUSD 完整剥离并私有归档

> 本条将历史数据继续用于配额计数的要求由 D19 取代；MON 单资产、私有归档及不扩大资金权限仍有效。

- **背景：** 用户最新明确旧 dUSD 完全不要、从代码中剥离，覆盖 D16 保留旧 UI/兼容读取的方案；不改变已发生的链上事实。
- **选择：** 产品只保留 MON 合约、前端、ABI、接口与脚本；删除 DemoUSD 源码/测试及旧 dUSD 资金入口/读取兼容。旧订单和凭证整账本放服务器私有备份，旧公开回执仅作历史存档、源代码由 Git 历史保存。
- **原因：** 避免产品同时出现两套资产和误导性操作入口，按用户要求收窄为一种支付资产。
- **后果：** 新活跃账本只存 native MON 订单，旧 API Key 和平台 session 不迁入；Para 钱包本身不变，用户可能需要重新签名登录并创建新 MON Key。仅携带旧订单 buyer + createdAt 的最小配额历史，保持固定 epoch 和原有限额，不能因清理账本重置次数。旧链上资产不可删除，本轮不销毁、兑换或代用户提款。
- **状态与依据：** 用户明确剥离要求；根调整后端/store/切换，合约 agent 删除旧代币源码测试，前端 agent 删除旧 UI/ABI，旧脚本退出当前工作树。MON-only 根 91/91、合约 41/41、前端最终 8/8 与类型/lint/构建通过。公网已切 a78470a、配置/模型回读 MON/18 正确；旧 15 单整账本私有备份，新账本仅导入最小配额历史，公网 API/SSE 一单与浏览器登录/账户读取通过，浏览器 MON 付款未验。D16 兼容层通过记录仅属历史。


### D18 — 仅清理 Git 仓库及历史中的演示域名

- **背景：** 用户先要求盘点域名引用和历史清理可行性，收到报告后明确“只处理 git 仓库和 git 历史”。这是在 D15 公网部署决定之后新增的公开资料处理要求，不撤销 D15 的线上部署，也不更换实际演示入口。
- **最终选择：** 清理 Git 跟踪文件及可控提交历史中的目标域名；公开说明采用 `demo.example.com` 脱敏占位。用户后续明确“PPT 不需要管，PPT 不上传到仓库”，覆盖此前净化 PPT 后继续跟踪的中间方案：两版 PPTX 与对应 PDF 从当前跟踪及全部历史移除并加入 ignore，本地内容不再处理，用户当前编辑保持原样且未重新审阅。新克隆不含这 4 个演示文件，文档改为本地产物路径说明。重写历史与远端更新已包含在授权中。
- **原因：** 只改当前版本无法移除旧提交中的地址；按用户划定范围清理公开仓库，同时保持线上项目可用。
- **后果：** 相关提交及后继提交 SHA 改变，旧 SHA 和服务器 release 名仅作历史标签；不编造新旧映射。已有副本须重新克隆或完成受控迁移，不能直接 merge/push 旧历史。示例域名不表示已上线或验收过的新网址；公开回执只替换地址展示，链上地址、哈希、金额及验证结果不变。
- **范围限制：** 不修改 MOJO 实际字段、服务器、DNS/HTTPS、Para、Git ignored 文件、第三方副本或缓存，不清理 IP 等其他数据。不能保证外部旧 SHA 缓存或已下载副本完全消失。
- **状态与依据：** 2026-09-05（Asia/Shanghai），最终历史已通过 SSH 精确 lease 推送到 `794139cd5d90a36c2e7c61e362c0afd433fbcf99`。独立 GitHub 全新克隆与清理后的本地仓库均为 23 个提交、643 个对象，目标域名及 PPT/PDF 路径/对象零命中；原 `.git` 的旧 refs/reflog/对象清理和 `git fsck --full` 通过。4 个本地演示文件移除前后 SHA256 一致，仅保留为 ignored 文件；不改用户编辑。此前 a4842c7 为中间阶段，最终完整证据见 [域名引用盘点](domain-reference-audit.md)。完成回执文档另行归档。

### D19 — 取消演示次数与额外并发限制

- **背景：** 用户追问“还剩 1 次”的来源。只读核对未找到用户指定每日 6 次、全场 10 次的记录；这些是 D14 公网准备阶段为约束 Router 测试 MON 消耗加入的工程保护，并非 Monad、钱包或赛事要求。
- **明确选择：** 用户要求“取消吧，不受限制，提交代码并更新线上”。取消每日/累计次数、每钱包/全局额外 Demo 并发门槛及相关演示准入配置；仍遵守单笔预算、可用余额、钱包消费授权和卖家真实 capacity。取消演示计数不代表免费请求、无限算力或无限资金授权。
- **替代关系：** 本条取代 D14 的次数/额外并发策略，以及 D17 将最小历史用于继续计算配额的要求；D14 的代理信任边界、D17 的 MON 单资产和旧资产私有归档继续有效。
- **数据与权限：** 保留当前订单、幂等记录、凭证及已有 history，不删除历史以制造额度；原 history 此后只作历史，不再参与请求计数。不改合约、单笔预算或用户资金授权，不额外存款/授权/提款。
- **状态与依据：** 2026-09-05（Asia/Shanghai），用户已明确授权实现、提交及上线；根和 Router agent 已完成代码修改与本地回归，待受控发布。当前线上仍为 fc646d8，取消限制尚未部署。当前代码已删除 DemoAdmission/DEMO_LIMITS、Engine 第四构造参数及三项 DEMO_* 解析，不引入新暂停机制；旧一次性迁移脚本退役，常规发布保留原生账本。根 97/97、根及新测试类型检查通过；新增 request-guards 10 项替换旧限额测试，详见[进度](progress.md)，上线结果待补。D18 脱敏与本地演示文件要求继续生效。
