# InferPool 五分钟上台速查

> PPT/PDF 仅保留本地，不随 Git 仓库提供；文件位置见 [本地演示材料说明](../artifacts/presentation/README.md)。最新 WPS v2 已做只读内容/XML与画布边界检查，未做新的全屏渲染验收；备用 PDF 落后于当前 PPT，须从最终 WPS 文件重新导出。

> 地址脱敏说明：`demo.example.com` 为占位域名，不是实际部署或验收地址；本文历史验证记录指向清理前的真实地址。

**两分钟讲三页 PPT，三分钟演示网页。现场只发一笔正常请求；卖家故障展示先前已确认订单。** 入口：[demo.example.com](https://demo.example.com)。MOJO [#385](https://mojo.devnads.com/projects/385) 已提交、待审核。

当前主讲使用本地 `InferPool-pitch-v2.pptx`：3页主线完整，每页约40秒抓重点，共2分钟。WPS保存于17:18:46，现有PDF为17:14:39前稿；先从最终WPS导出同步PDF备用，再做一次5分钟计时彩排。[文件说明](../artifacts/presentation/README.md)保留检查边界。

## 上台前准备

- 用已登录的 **Chrome Para B**，先打开“推理市场”和“请求账单”。确认 Router 在线、“Monad 演示节点”的 **Reasoner** 可接单；不要把应用内浏览器的访客状态当作 Chrome 已登录。
- 18:35:59 固定块 59882957 核对：托管 **0.0990148 MON**、剩余授权 **0.0491806 MON**，有效至 **9月6日17:22:16（+08）**；B钱包Gas **0.785624238 MON**、Router Gas **100.250057574 MON**，无锁款，足以覆盖本次0.001预算。临上台重读，不需重复存款或授权。
- 18:35–18:37只读核对：23cc7f8、两服务active、A normal/2槽、8单/0未决；Chrome正式演示tab中B已登录，Reasoner、0.001预算/512输出/缓存关闭已准备。最近用户订单 `7d9a66ea…` 展示新回答及已确认账单，费用0.000161、释放0.000839 MON；本轮未发新单。
- 先定位正常历史单 **d6f9abd0…** 与故障历史单 **c985df51…**。把[正常账单图](../artifacts/submission/native-normal-bill.jpg)、[两单对照图](../artifacts/submission/native-bills-comparison.jpg)和[失败详情图](../artifacts/submission/native-failure-bill.jpg)打开作备用。
- D19 已在 0a82030 取消演示次数与额外并发限制，部署证据见[进度](progress.md)。五分钟流程只安排一笔正常请求是节奏选择，不是产品次数上限；确认预算、钱包授权和节点容量，节点保持 normal，不做 SSH 故障注入，不展示验证码、API Key 或终端凭证。

## 五分钟时间与讲稿

| 时间 | 页面 | 可以照读 |
| --- | --- | --- |
| 0:00–0:40 | PPT 第 1 页：供需价值 | “我们的目标是让闲置推理能力变成可按需购买的服务：卖家报价接单，买家比较价格、按量购买，减少闲置并降低使用成本。用 Token 计量，用 MON 结算，让供给和需求更容易找到彼此。” |
| 0:40–1:20 | PPT 第 2 页：Web3 与 Monad | “Monad 的预算托管、资金结算和 EVM 钱包工具支持这个市场。反过来，InferPool 希望为生态应用和 Agent 提供统一的推理采购入口：需求吸引更多供给，也让 MON 用于实际的按需服务支付，形成双向价值。” |
| 1:20–2:00 | PPT 第 3 页：使用流程 | “卖家运行客户端、签名接入并报价；买家连接钱包、存 MON、设消费额度和期限，再选节点和单次预算。系统锁预算、执行、结算、释放余款。下面看一笔正常请求，再看已确认的零费故障账单；当前使用固定演示响应和 Unicode 计量，Router 负责计量和判责。” |
| 2:00–3:30 | 网页：正常单 | 按下方步骤只点一次运行，等待“链上已确认”，显示费用、释放预算和交易链接。 |
| 3:30–4:30 | 网页：历史故障单 | “这是**先前验收时注入故障的订单**。已经输出 48 个计量单位，但卖家失败后整单推理费是零，0.001 MON 预算全部释放。” |
| 4:30–5:00 | 对照与收尾 | “今天验证的是资金与请求链路：正常按量付费，卖家失败免推理费。后续再接真实推理供给，检验更低价格和资源利用率的目标；Gas 是单独的链上成本。” |

## 现场网页点击流程

1. 点击左侧 **“推理市场”** → 节点选择 **“Monad 演示节点”**（`seller-monad`），模型显示 **Reasoner**。不要临场改成自动匹配或新增卖家。
2. **本次预算：`0.001` MON；最多输出：`512`；取消勾选“上下文缓存”。** 输入：

   > 用三个要点解释：链上托管如何保护买家的推理预算？

3. 点击一次 **“运行请求”**。先看到“正在提交与确认预算”，锁款后再输出；等待右侧 **“链上已确认”**。指出“实际费用”“已释放预算”，再点 **“查看结算交易”**。新单以现场账单为准，不把历史费用贴成新结果。
4. 点击 **“请求账单”**，选择故障单 **`c985df51-7600-43dc-8ac2-5a9fcf2b150f`**。确认费用 **0 MON**、释放 **0.001 MON**，明确这是先前已验证的故障单；不要点击运行去重造故障。
5. 回到两单对照或 PPT 收尾。正常费计入卖家**合约托管可用余额**，不是自动提现到外部钱包。

## 出问题就这样切换

- **登录、余额或额度未准备好：** 不在台上开户/领币/重新授权，直接展示历史正常单 `d6f9abd0-b3c2-4169-93b1-92509e304426`，说明“这是之前的测试网验收记录”。
- **新单约 30 秒仍未确认、断网或响应卡住：** 记住请求 ID，显示真实待处理状态；不要再点新请求，不换幂等 Key，不以关闭页面当取消。切到上述正常历史账单，再讲历史故障单。
- **网页或区块浏览器不可用：** 展示本地正常/对照/故障原图和[六笔交易 JSON](../contracts/deployments/inferpool-native-browser.json)，明确是历史证据。正式现场请求恢复后仍应查原单结果。

浏览器已验证操作、完成和账单，**未采集逐帧输出增长证据**；公网 API 的 34 批 SSE 是另一次验证。当前不临场演示取消、提款、撤销、回收、钱包切换或断连恢复。完整依据见[进度](progress.md)；扩展说明见[演示指南](demo-guide.md)。

价格答问：“低价取决于真实供给和 Gas，逐单链上成本仍待优化；我们尚未证明微支付经济性。”
