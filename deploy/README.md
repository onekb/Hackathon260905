# Public deployment and HTTPS handoff

> 地址脱敏说明：`demo.example.com` 为占位域名，不是实际部署或验收地址；本文历史验证记录指向清理前的真实地址。

**Current release 0a82030: Demo admission limits are removed (D19).** The owner explicitly requested unrestricted request counts and removal of extra demo concurrency gates. Keep per-request budgets, wallet grants and seller capacity; preserve the active ledger, credentials and history. The new interface removes DemoAdmission/DEMO_LIMITS and the three DEMO_* settings, with no new pause switch. The retired one-time switch-native.py migration refuses execution. Root tests 97/97 and type checks pass; the retired migration exits before state access, and the environment filter was checked to remove only the three retired settings while rollback preserves the ledger. Web and contracts are unchanged; reuse fc646d8 static assets. At 18:00:57 Asia/Shanghai the controlled release began; both services were subsequently stopped and started, and the version was switched. Linux npm ci and staged request-guards/native-ledger tests 18/18 passed. At 18:01:40 source hashes matched locally and all three old settings were absent; ledger bytes and credentials/idempotency/history were preserved. No real requests or chain transactions were issued. See [progress](../docs/progress.md).

**Streaming repair fc646d8 is deployed; a browser request and its settlement are verified (2026-09-05).** The patch removes cross-order settlement blocking in Provider message dispatch and prevents older Web polling snapshots from rolling back visible output. Root tests 97/97, Web tests 13/13, both type checks, Web lint and the public static export passed. Final read-only state is five orders, zero unresolved and 15 quota-history entries with the original epoch; four global attempts and one buyer B daily attempt remained at that pre-D19 checkpoint. This is a regular code release: retain the active native ledger and credentials; do not rerun the one-time `switch-native.py` asset migration. SSE protocol and proxy configuration are unchanged. Both services were stopped and started under control. Public home/health/config/models return 200, and local/server/public index hashes match. The original orders, credentials, history and epoch are retained, env bytes are unchanged, and the ledger remains 0600. Chrome B showed the budget-confirmation message, final output and confirmed bill for request 317077c8-9340-4bc3-a742-ca64091e27c6 (.0001658 MON charged, .0008342 released); independent RPC verified both receipts. Sampling did not capture partial text, so frame-by-frame public rendering remains unverified. See [progress](../docs/progress.md).

**Current release: 0a82030 serves native MON/18; the URL below is a redacted placeholder.** Remote npm ci completed successfully; public config/models return 200 and seller A is online with two slots, v1 quote .3/.03/.375/.8 MON per million and minimum .000001. One public API request passed 34 incremental SSE batches, confirmed settlement, idempotent replay and temporary-key revocation; A is buyer/seller/Router. Chrome B login/account reads passed, and B subsequently completed .1 MON deposit, .05 MON/24h grant and normal/failure browser requests with six transactions independently verified. The sections below include the initial dUSD deployment as explicitly historical evidence.

These files are templates plus a deployment progress record, **not evidence of complete public Demo acceptance**. The owner has configured HTTPS for `demo.example.com`, forwarding the whole site to `127.0.0.1:8788` through the existing same-host OpenResty proxy. Certificate validation passes. The backend has now started, and both loopback and public `/health` return 200; the earlier 502 is resolved. Public Web pages, configuration and seller A authentication over WSS have passed. The Para login modal also opens without initialization error; the later MON release separately passed public API/SSE settlement and Chrome B signature login/account reads; B browser normal/failure payments subsequently passed, without frame-by-frame streaming capture. The application team has inspected the proxy read-only and has not changed certificate, nginx or 1Panel settings.

**Initial dUSD deployment history (superseded by a78470a):** On 2026-09-05, remote DNS resolved the selected domain to the intended host. Node.js v22.23.2 and `@alchemy/cli@0.24.0` were independently installed; Linux `npm ci` completed with 775 packages and the CLI installation added 178 packages. The initial release was `/srv/inferpool/releases/319c6b9`, linked from `/srv/inferpool/current`, with the matching ABI and Web export built for `https://demo.example.com`. Release files are root-owned and read-only to the service user. The shared-port code passed review, 82 tests and type checks; commit `319c6b9` has been pushed to `origin/main` over SSH.

Both service units and private environment files are installed. `systemd-analyze verify` exited 0, with compatibility warnings concerning existing host units and no new-unit errors; those existing units were not changed. `daemon-reload` completed. Following signer approval and ledger migration, the Router was enabled and started, with systemd active/running and NRestarts 0. Seller A is also enabled and running; both services report NRestarts 0 and ExecMainStatus 0. The configured fixed admission epoch is `2026-09-05T06:22:02Z`. New-device login completed successfully. The first wallet-session request expired after five minutes without approval (exit 6); verification reported `valid=false`, `status=none`. The replacement `inferpool-router-linux` request, started without `--force`, was approved: connect exited 0 and an independent status verification confirmed the original Router address and valid transaction/message signing permissions. The approved expiry is `2026-09-12T06:33:39.043Z`; config mode is 600. The old local Router is stopped and no longer listens on 8788. Its 14-order ledger was privately backed up, copied and SHA256-checked before startup, retaining 13 confirmed orders and one definite lock failure; the target ledger is owned by inferpool with mode 600. No Mac login/session files were copied. Package installation, unit validation and public artifact checks do not establish signer or service readiness; exact hashes and checks are in [docs/progress.md](../docs/progress.md).

A read-only check run as the actual Linux service user exited 0: static export path, fixed admission epoch, loopback proxy mode, Monad chain 10143 and the contract's fixed Router address all matched. `router.env` is mode 600. Router gas was `0.992516012 MON` at block `59833890`. No signing or broadcast occurred; this snapshot does not establish wallet-session approval or future gas sufficiency.

## Runtime choice

The Web is exported as static files. The Express 5 Router requires one long-running Node.js 22+ process, an authorized Alchemy CLI session, and a persistent private ledger directory. With `WEB_STATIC_DIR` it serves Web, HTTP APIs, SSE and WebSocket together. No Next.js server is needed. Keep one Router for the existing deployed Router identity and ledger; do not launch a second copy while the local instance can still accept orders.

The selected layout uses **one upstream for every path**: the owner's HTTPS proxy forwards to `http://127.0.0.1:8788`. The existing OpenResty instance has been checked to use host networking, so its loopback reaches the application on this host. A proxy in a different container network would need a different, explicitly checked upstream; do not generalize this loopback address to every proxy installation. A temporary tunnel alone does not satisfy the competition's long-term availability requirement.

Cloudflare Quick Tunnels do not support the SSE stream this app uses. Render Free cannot persist the Router's local ledger across restarts. Those services require a different arrangement before they can host this backend safely. See [Cloudflare Tunnel documentation](https://developers.cloudflare.com/tunnel/setup/) and [Render Free limitations](https://render.com/docs/free).

## Native MON release: public API verified, browser login/read verified

Current source now uses the independently deployed native market `0x142a4904307244Bed0cECD72dE8329A253333182` ([deployment and verification](../contracts/deployments/inferpool-mon-native-testnet.json)). Native/MON/18/router/runtime checks and both explorer verifications passed. The original MON cutover selected `/srv/inferpool/releases/a78470a`; the current routine upgrade selects `/srv/inferpool/releases/0a82030` without repeating that migration. After stopping/reconciling the old writer, the cutover preserved the full 15-order ledger and credentials privately at `/srv/inferpool/state/backups/native-20260905T073750Z`; the old ledger hash was unchanged. The new `/srv/inferpool/state/router-mon-state.json` initially contains no old orders or credentials and only 15 minimal buyer/createdAt admissionHistory entries. The original cutover preserved epoch and limits; D19 subsequently removed these runtime limits and the old epoch configuration while retaining history. Live config/models verify native MON/18 and seller A's new quote. No asset conversion, burn or withdrawal was performed by this data migration.

Use `node scripts/deploy-mon-native.mjs` for read-only preflight, `--deploy` for explicit deployment/recovery, and `--verify-only` for existing-code/source verification. `node --import tsx scripts/native-monad.ts` is read-only by default; `--execute --smoke` performs the bounded same-wallet MON test documented in [the runbook](../docs/runbook.md#原生-mon-测试网当前源码). Current npm setup:monad and test:monad point to native-monad --execute and --execute --smoke; old dUSD scripts are removed from the active project and retained in Git history. The new test:api:monad invokes smoke-native-api.ts --execute; its one-request public run passed; [evidence](../contracts/deployments/inferpool-native-api-smoke.json) records .0001285 MON, 34 SSE batches, confirmed receipts, replay and key revocation. The output reached max_tokens 128 without consuming the full .001 MON budget. The default read-only rerun reports alreadyVerified=true and does not create another order. Do not use historical scripts to initialize the new market.

The D17 one-time cutover created the MON-only ledger and retained minimal buyer/createdAt history. That migration is complete and its script is retired. For routine releases, reconcile and stop the current writer, privately back up the full native ledger and credentials, and preserve them while switching code. D19 requires retaining history without counting it toward request limits; do not empty the ledger or repeat asset migration. No conversion, burn, withdrawal or spending-grant change is part of this release.

Stage matching code, native ABI and the rebuilt Web together. Read `/config` back as new market/MON/18 without legacy fields, then reauthenticate seller A with its new-market quote. Native UI defaults are .1 MON deposit, .05 MON /24h grant and .001 MON request budget; they are not copies of old dUSD amounts. The smaller same-wallet contract smoke is .01/.005/.001 and has passed eight real testnet transactions: normal charge .000110 MON, seller failure zero, withdrawal .001, final escrow .009 and no locks ([evidence](../contracts/deployments/inferpool-native-monad-smoke.json)). This is separate from buyer Web approval and public HTTP/SSE acceptance. Source tests/builds do not establish new-origin wallet or streaming acceptance. No legacy UI, ABI or compatibility reads remain in the product. Users may need to sign in again and create new MON API keys; the Para wallet itself is unchanged.

The [post-cutover read-only summary](../contracts/deployments/inferpool-native-public-deployment.json) records one native confirmed order, zero pending, 15 quota-history entries, two attempts in the unchanged epoch and eight remaining. Both services are active, the new ledger is 0600, no legacy runtime env fields remain, and the old ledger SHA is unchanged. At that checkpoint the running release remained a78470a; the maintenance scripts and earlier evidence/docs were archived and SSH-pushed in abefc2d.

Latest independent-browser evidence is [inferpool-native-browser.json](../contracts/deployments/inferpool-native-browser.json): normal .0001658 MON, failure zero, B escrow .0998342, remaining grant .0498342, seller A escrow .0091658, all locks zero. Root's 07:58:23Z read-only service check found normal/online, active0, slots2 and no lastError; native orders are now three, quota history15, global attempts remaining6 and B daily remaining3, with the same epoch. The prior deployment summary's remaining8 is historical. At that earlier checkpoint code remained a78470a; the proof/screenshots/docs were archived and SSH-pushed in fc9170f, without redeployment.

## Build the frontend

Set the real public Router HTTPS origin and the existing **frontend** Para key in the ignored `web/.env.local` (or your build environment). Do not use a secret Para key, Alchemy session, RPC credential or wallet key in any `NEXT_PUBLIC_` variable.

```sh
INFERPOOL_STATIC_EXPORT=true INFERPOOL_PUBLIC_BUILD=true npm run build --workspace web -- --webpack
```

This writes only deployable Web assets to `web/out/`; it does not export the Router or private ledger. The public-build check requires a multi-label HTTPS DNS origin and the Para frontend key; it rejects IP literals (including mapped IPv6), local names, credentials and URL paths. It does not verify DNS resolution or service reachability; check those separately. Changing `NEXT_PUBLIC_ROUTER_URL` requires another build. Regular `npm run dev:web` / `npm run build:web` retain the existing Next.js behavior when these flags are absent.

The explicit webpack path was used for local export validation because this machine's Turbopack CSS helper could not bind its compilation port. The Para package emits warnings for optional account-abstraction integrations that this app does not enable; the build still completes. Public wallet and API checks remain required after the actual public origin is configured.

The two routes are `/` and `/provider-connect/`. Serve real exported files and route directories; do not rewrite API errors to `index.html`. Check the new frontend origin in the Para project configuration and verify wallet login after deployment.

## Prepare the application service

1. Choose the permanent public origin and persistent service directory. Install Node dependencies and the Alchemy CLI in the service user's environment. The Router also reads `contracts/out/InferenceMarket.sol/InferenceMarket.json`, which is ignored by Git and is **not** generated by `npm ci`. On the build host, install Foundry, run `npm run setup:contracts` and `forge build --root contracts`, then include that matching public ABI artifact at the same relative path in the release (or compile it on the server). Authorize the same intended Router identity through the supported session flow; never put a session token into a public image, repository or command transcript.
2. Before moving the existing service, stop admission of new orders and reconcile every in-flight/uncertain order. Stop the old Router, take a private backup, and move its ledger with owner-only permissions. Do not start the target with an empty replacement ledger.
3. Use `/srv/inferpool/current` to select the final release. Copy `router.env.example` to `/srv/inferpool/state/router.env`, with owner `inferpool` and mode 0600. For the D19 release, remove the retired `DEMO_ADMISSION_ENABLED`, `DEMO_ADMISSION_START_UTC` and `DEMO_NEW_ORDERS_ENABLED` settings; the new code does not read them. `WEB_STATIC_DIR=/srv/inferpool/current/web/out` must refer to a valid public export; keep it separate from the private state. Request and funding boundaries are in [Router README](../server/README.md).
4. Start **one** service from the repository root with its authorized Alchemy session available on `PATH`:

   ```sh
   /opt/inferpool/node/bin/node --env-file=/srv/inferpool/state/router.env --import tsx server/src/index.ts
   ```

5. Install and verify the supplied service units after preparing their configuration, but keep them stopped until the remote signer is approved and the original ledger has been migrated. Validate the installed units on Linux with `systemd-analyze verify` before `daemon-reload`. This target has completed verification and reload; Router enable/start subsequently completed after signer and ledger checks. Seller A subsequently passed live WSS authentication through the public domain. Existing-host compatibility warnings were left untouched and no new-unit errors were reported.
6. Hand the single HTTP endpoint to the owner for the HTTPS configuration below. After HTTPS works, update each seller's `--router` to `wss://demo.example.com/provider`. The public domain is part of the challenge, so fresh seller authentication is required. A loopback `ws://` URL fails the seller's exact domain check and cannot substitute for this step. The browser-wallet seller's `--wallet-ui` must use a Web build pointing to the same Router origin. Keep seller consoles on loopback, outside the proxy.

| Template / path | Purpose |
| --- | --- |
| [inferpool-router.service.example](inferpool-router.service.example) | Router unit, runtime user `inferpool`, selected release `/srv/inferpool/current` |
| [inferpool-provider.service.example](inferpool-provider.service.example) | Seller A using `--alchemy-session`; requires working public WSS, console only on loopback 8793 |
| [router.env.example](router.env.example) / [provider.env.example](provider.env.example) | Separate owner-only environment files; both use the same new Linux signing session |
| `/srv/inferpool/state` | Mode 0700 private directory; active MON ledger `router-mon-state.json`; old ledger only in private backup, signer config `alchemy/config.json`, environment files mode 0600 |
| `/opt/inferpool/node/bin/node` / `/opt/inferpool/tools/bin/alchemy` | Independent Node runtime and Alchemy CLI 0.24.0 tooling paths; verify installed versions before use |

Both units run as `inferpool`; sharing that user and state access is not process-level credential isolation. Plan restarts by reconciling in-flight orders and controlling the service stop; the D19 release has no Demo pause configuration. Router shutdown has a 240-second grace period and Provider 20 seconds, but these values do not replace ledger reconciliation. The first MON switch restarted only Router and waited on a long-lived Provider connection; controlled Provider stop/start let it exit normally without a force kill. That migration script was later changed to restart both services together and is now retired; routine releases must preserve the active native ledger without rerunning it. Never start with an empty ledger as a shortcut.

## HTTPS proxy handoff to the owner

The owner handles TLS and the existing proxy configuration. The application team provides the following values; the endpoint has passed page/API-read and seller WSS checks, and a later native API request passed incremental SSE/settlement; B browser normal/failure payments also passed; browser frame-by-frame streaming remains outside acceptance.

| Setting | Value / requirement |
| --- | --- |
| Public origin | `https://demo.example.com` |
| HTTP upstream | `http://127.0.0.1:8788` on the same host |
| Paths | Forward the entire site without stripping paths: Web pages, `/_next/` assets, APIs and `/provider` all share this upstream |
| WebSocket | Preserve HTTP/1.1 Upgrade/Connection handling for `/provider` |
| SSE | Disable response buffering/caching and permit a long upstream read interval; do not automatically retry write requests |
| Forwarded identity | Preserve the public host/protocol and overwrite `X-Forwarded-For` with the verified client address; do not trust a client-supplied chain |
| Router reachability | Keep 8788 bound to loopback; `ROUTER_TRUST_PROXY=loopback` assumes the checked same-host proxy, not arbitrary remote forwarding |

`nginx.conf.example` remains an uninstalled reference for a split static/API layout. Do not apply it unchanged to this selected whole-site single-upstream layout or overwrite the owner's existing sites. The owner has configured this proxy; the application team has made no TLS/1Panel changes.

Read-only checks on 2026-09-05 confirmed default certificate validation with both remote Python urllib and curl, and a certificate SAN of `demo.example.com`. HTTP/1.1, Upgrade, Host and HTTPS scheme are configured; `Connection=$http_connection` forwards the incoming header. These configuration checks were subsequently complemented by actual seller A WSS authentication; native API streaming was subsequently verified with 34 incremental batches. The initial 502 responses were observed before backend startup; loopback and public `/health` now return 200 with `ok:true`, `chain_mode:monad-testnet`, `mock_inference:true` and, before seller startup, `providers:0`.

The owner’s screenshot shows caching disabled, but the inspected site configuration has no explicit buffering, cache or read-timeout directives. Before public acceptance, the owner should verify `proxy_set_header X-Forwarded-For $remote_addr`, `proxy_read_timeout 300s`, `proxy_buffering off`, `proxy_cache off` and `proxy_next_upstream off` in the selected proxy location. These are proposed adjustments, not changes already made. The application's `X-Accel-Buffering: no` SSE header can disable buffering unless an upstream configuration ignores it; verify actual incremental delivery after startup.

The owner’s proxy should disable response buffering and retrying writes, preserve WebSocket upgrade headers, and allow at least 240 seconds without upstream data. This includes the initial on-chain reservation wait before SSE headers are sent. See [nginx WebSocket proxying](https://nginx.org/en/docs/http/websocket.html) and [proxy module documentation](https://nginx.org/en/docs/http/ngx_http_proxy_module.html).

### A separate authorized signing session on the server

Use the official [Alchemy headless login flow](https://www.alchemy.com/docs/alchemy-cli#headless-and-remote-environments) with the service user's private configuration directory. The owner completes login and wallet-session approval in their browser. The installed local CLI version verified for these commands is `@alchemy/cli@0.24.0`.

```sh
ALCHEMY_CONFIG=/srv/inferpool/state/alchemy/config.json alchemy auth login --device-code
ALCHEMY_CONFIG=/srv/inferpool/state/alchemy/config.json alchemy wallet connect --mode session --instance-name inferpool-router-linux
ALCHEMY_CONFIG=/srv/inferpool/state/alchemy/config.json alchemy --json --no-interactive wallet status --verify
```

Run these as the same service user, not through a different home/configuration. Do not add `--force`. Verify the approved wallet is exactly the existing Router address `0xAc801eEC099C65A605B809b98A09A62674614A08`, the session is valid and permits `evm.signTransaction`, and record only its non-secret expiry. The contract fixes the Router identity; changing an environment variable to another wallet does not replace it. `ALCHEMY_AUTH_TOKEN` alone is not a delegated wallet session.

The session expires (CLI default request: seven days; the approved expiry is authoritative), so persistent hosting still requires renewal and monitoring. Do not copy the Mac's authentication files to the server as a shortcut. The CLI installation and new-device login are complete; the login token is retained only in the private server configuration. The first wallet-session request timed out; verification confirmed no session before a fresh request was started without `--force`. The owner approved the replacement request, and independent verification confirmed a valid session for the fixed Router, expiring `2026-09-12T06:33:39.043Z`. No further device login was needed. Session readiness and the later public native API transaction have separate evidence; session approval itself does not prove a completed request. Device codes and credential-bearing approval URLs must not enter these documents.

## Acceptance and remaining checks

The root agent verified public `/`, `/provider-connect/`, an actual `/_next/` JavaScript asset, `/config` and `/v1/models` with HTTP 200. Config reports Monad 10143 and the expected contracts; Chrome rendered the marketplace with Router online and one seller. `/v1/unknown`, `/missing-page` and `/.env` returned JSON 404. Seller A is normal with two slots, active quote v1 (`30/3/37.5/80`, minimum reserve `0.0001`), and its loopback console reports the public WSS URL, no error and matching effective pricing. The old local Alchemy seller is stopped. Browser-wallet seller B is currently offline; this is one persistent public seller, not two.

The Para Sign Up or Login modal opened in Chrome without initialization error and was closed without login or signing. An original guest-market screenshot was saved and visually checked at [artifacts/submission/inferpool-public-market.jpg](../artifacts/submission/inferpool-public-market.jpg). No new wallet login or inference transaction was performed on the public origin in this deployment round. If extending acceptance, verify new-origin wallet login, one bounded inference request with incremental SSE and its receipt, exact idempotent replay, and request-limit rejection without consuming funds. Earlier localhost/testnet evidence does not establish those public paths.

Record the actual URLs, deployment time, host lifetime, verified checks and limitations in `docs/progress.md`. Keep the private ledger, raw authentication data and local console addresses out of public artifacts.
