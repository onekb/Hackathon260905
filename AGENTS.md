# InferPool 协作与文档规则

这些规则来自用户的明确要求：边开发边完善 `docs/`，把后续对话中的决定同步保存，让后来接手的人能了解项目始末。

## 开始工作

- 先读 [docs/README.md](docs/README.md) 和 [docs/progress.md](docs/progress.md)，再读本次涉及的需求、架构或运行文档。
- 查看当前文件与 Git 状态，保护用户和其他 agent 的工作；不要覆盖不属于本次任务的修改。子目录的 `AGENTS.md` 仍适用于相应目录。
- 以可检查的源码、测试输出和部署回执判断实现状态。不要把计划、已有脚手架、登录成功或启动进程写成完整功能验收通过。

## 每轮开发同步

- 本轮只要影响功能、接口、计费、架构、资金权限、部署或需求，就在本轮完成前更新相应 `docs/` 文档，并更新 `docs/progress.md` 的状态、证据及下一步。
- 重要对话提炼到 `docs/conversation-log.md`：记录用户要求、采用的解释、结果、未决事项和关联决策。不要复制整段聊天；不要凭编号答案补造缺失问题。
- 新的取舍或既有决定发生变化时，追加 `docs/requirements-and-decisions.md` 决策记录，写明背景、选择、原因、后果和依据。修改已有决定时保留被替代关系，不能抹去历史原因。
- 可以委托专职文档 agent，但主 agent 仍负责把新里程碑、失败、范围变化和验证结果同步给它，并在结束前核对文档已经更新。没有可用子 agent 时直接维护。
- 所有“通过”“已部署”“已上线”表述都要说明验证环境和证据；区分本地 Anvil、Monad 测试网、浏览器交互验证与公网部署。命令存在不等于命令已成功执行。

## 完成前核对

- 核对文档与实际实现一致，尤其是接口字段、默认端口、链 ID、合约地址、计费规则和当前未完成项。
- 根 README 保持简明入口；详细事实集中在 `docs/` 和组件 README，减少相互矛盾的重复内容。
- 时间使用实际日期并说明时区；不知道旧事件的精确时间时按顺序记录，不编造时间。
- 不在文档、日志、截图或提交中记录私钥、助记词、会话令牌、完整 API Key、个人登录邮箱或带凭证的 RPC URL。公开合约地址与交易哈希可作为证据。
- 纯文档修改无需新增或运行业务测试；检查链接、命令与事实即可。代码修改运行与风险相称的验证，并把结果和限制记入进度文档。

文档同步属于开发工作的一部分，不需要额外确认；不要为此创建定时任务或宣称无人运行时也会自动维护。
