# AI 推理市场：黑客松首版规格

状态：开发基线规格。本文保留最初的范围和工程决定；最新实现进度、部署证据和后续讨论以 [docs/README.md](docs/README.md) 为入口。业务合约已部署 Monad 测试网，网页仍在联调，不能把规格条目直接等同于验收通过。

## 已确认的范围

- 开发窗口约 8 小时。
- 部署目标已确认：Monad 测试网。展示重点是可验证的 Web3 交易和资金规则。
- 买家同时拥有在线体验台和最小 API；两者共用订单、计量、预算与结算流程。
- 卖家自行运行独立节点或软件。首版仅输出 Mock AI 响应，不接真实模型、订阅套餐或算力机房。
- 平台 Router 可以中心化，负责路由、计量和故障判责。
- 买家请求平台统一 API；卖家节点主动通过 WSS 长连接接入平台，由平台转发请求与响应。卖家不需要提供公网接口地址。
- 输入、输出分别报价，考虑缓存计费。预算按单次请求计算，卖家可设置最低要求。
- 卖家故障：整单推理费为零，已输出部分也不收费。
- 买家主动取消：收取截至取消生效时已产生的实际费用，释放剩余额度，不收满预留金额。
- 请求、响应、上传内容和凭证不上链。
- TEE、假模型投诉及扣质押机制属于第二版方向。

## 首版工程建议

### 产品与运行方式

- Web：模型市场、在线体验台、钱包与授权、请求账单、API 接入说明。
- Router：请求验证、报价匹配、节点连接、Mock 用量归一化、预算控制、故障判责、链上结算。
- Provider：独立运行的进程及本地网页控制台，支持上线、下线、报价和故障演示开关。
- 合约：卖家注册与报价、原生测试 MON 托管、受限消费授权、订单锁款、结算、超时释放与提款。
- 按已确认要求部署到 Monad 测试网。现行 D17 仅使用原生测试 MON；去掉 ERC-20 approve，保留托管存款、Router 金额/期限授权，并为钱包交易预留 MON Gas。
- 所有 AI 输出、缓存效果和模型用量均标注为模拟。报价、锁款与资金结算计划走真实测试网交易。

立项时 `contracts/` 只有 Counter 示例；前序 DemoUSD 与 ERC-20 市场已部署、验证，现仅作历史存档。D17 删除旧代币源码、UI/ABI/脚本，当前原生 MON 市场已部署验证，公网已更新到 MON-only 23cc7f8，API/SSE 一单和浏览器登录/账户读取通过，独立买家 MON 网页存款/授权及正常、故障两单已验，逐帧 SSE 未采证；旧整账本私有备份、凭证不迁移，最小历史仍保留。D19 已授权取消每日/累计次数及额外 Demo 并发门槛，history 不再计次，已在 0a82030 上线并回读源码/配置确认；单笔预算、钱包授权和卖家容量仍适用。Counter 记录保留作历史参考，最新状态见 [进度](docs/progress.md)。

### 买家在线流程

1. 连接钱包，获得测试资金，存入托管合约。
2. 明确授权平台可消费的总额度及有效期；提款权保留给买家钱包。
3. 选择模型、指定卖家或选择自动匹配，设置单次预算。
4. 系统验证参数和余额，锁定报价与本次预算，链上锁款确认后才向卖家派单。
5. 展示流式 Mock 输出、卖家、预算、用量与请求状态。
6. 结束后展示责任原因、分类用量、计费金额、释放金额与结算交易。

自动匹配按本次请求的估算总成本比较，不能只按某一项 Token 单价排序。首版在派单前选择一个卖家；请求开始后不自动换卖家或重试，从而避免重复输入成本、重复扣款和责任混淆。

### 卖家流程

启动节点 → 连接平台 → 注册身份与模型 → 发布报价 → 上线接单 → 输出 Mock 响应 → 查看账单与收益。

演示控制：正常完成、接单后超时、首个输出前失败、输出中途失败、缓存命中。

### 连接与派单

- 平台提供买家 HTTPS/SSE 接口及卖家 WSS 接口，运行环境必须支持持续连接。
- 卖家主动建立 WSS 连接；钱包身份认证成功后，平台绑定钱包、provider_id 与当前连接。认证挑战含域名、一次性 nonce 和有效期，不能仅凭客户端上报钱包地址认定身份。
- 钱包地址用于身份和收款，provider_id 用于选择卖家，model_id 用于表示市场中的模型；三者不等同于网络接口地址。
- 平台通过心跳维护在线状态，并按卖家可用并发数派单。心跳超时或节点忙碌时停止分配新订单。
- 卖家重新连接必须重新认证。进行中的订单不能因为重连而静默重发；断线、锁款释放和订单恢复仍遵循单一终态及幂等规则。
- 同一请求的流式片段带请求 ID 和序号，用于避免重复计量。缓存效果仍按模型、买家和卖家边界模拟。
- WSS 保护传输链路；平台和接单卖家仍可看到请求内容。首版不宣称端到端内容保密。

### 最小 API

建议接口：

| 接口 | 功能 |
| --- | --- |
| `GET /v1/models` | 可用模型 |
| `POST /v1/chat/completions` | 创建请求，支持基础 messages、流式响应、max_tokens |
| `GET /v1/requests/{id}` | 查询状态、用量、最终费用及交易 |
| `POST /v1/requests/{id}/cancel` | 显式取消 |

- 增加 `max_spend` 和可选 `provider_id` 扩展参数；金额使用十进制字符串，避免浮点误差。
- 首版仅承诺基础聊天与流式调用，不承诺完整兼容所有 SDK、工具调用、多模态或 Responses API。
- 买家在网页完成钱包认证与消费授权，再生成 API Key；调用程序不需要钱包私钥。
- API Key 只能关联到当前账户的有效消费授权，不能授权提款、修改钱包权限或突破链上总消费上限。
- 钱包登录签名包含域名、一次性 nonce 和有效期；API Key 使用随机秘密、只展示一次、服务端保存哈希，可撤销。
- 多个 Key 共用账户链上总限额；若提供单 Key 限额，它属于平台执行的附加限制，不能声称由链上单独保证。
- 支持 `Idempotency-Key`；相同账户下重复请求返回同一订单，相同 Key 携带不同内容应拒绝。
- 流式响应提供请求 ID，断开后可查询最终状态；关闭连接不等同于调用取消接口。
- 网页和 API 调用同一套后端流程，不维护两份计费逻辑。

## 计价与预算

报价包含普通输入、缓存读取、缓存写入、输出四项，统一以每百万 Token 标价。不支持缓存的模型不启用缓存项目。

普通输入、缓存读取和缓存写入的计费数量互不重叠。缓存 Mock 以买家、卖家、模型和上下文为边界，不假定换卖家后缓存仍有效。

建议把卖家的最低要求解释为“最低预留金额”，而非最低消费。预留 0.10、实际消费 0.02，则收取 0.02。

每个订单保存开始时的报价快照，之后改价不影响此单。预计账单与已结算账单应明确区分。

- 费用以固定精度整数计算，统一金额精度和舍入规则。
- 总收费始终不大于本次锁定预算，合约也必须执行该上限。
- 锁款前按可能发生的输入成本保守检查预算，包含可能高于普通输入费的缓存写入，不提前假定命中缓存。
- 输出前检查下一步是否会超过预算；达到预算上限属于正常终止，不是卖家故障。
- 首版不叠加平台抽成或最低消费，避免与已确认的用量收费混淆。

## 责任与终态

| 情况 | 推理费 | 剩余锁款 |
| --- | --- | --- |
| 正常完成 | 实际用量 | 全部释放 |
| 达到预算上限 | 实际用量，不超过预算 | 全部释放 |
| 买家显式取消 | 取消生效前实际用量 | 全部释放 |
| 卖家错误、超时、部分输出后失败 | 0 | 全部释放 |
| 平台错误或无法可靠判责 | 建议为 0 | 全部释放或等待超时取回 |
| 锁款前参数、余额、最低预留检查不通过 | 不成立订单 | 不锁款 |

“已产生的实际用量”包括本次已计量的输入、缓存和输出，不仅是屏幕上输出的文字。

关闭页面或网络断连不直接判为买家责任。任务可在原预算内继续，买家可重新查询；这时最终账单取决于任务结果。

平台对每笔订单串行处理终态事件并持久化：卖家失败先成为终态则全免；买家取消先成为终态则按取消时用量收费，后续取消导致的连接中断不改判卖家故障。不得仅凭客户端自报成功或失败结算。

推理结果与链上结算状态分开：结算交易暂时失败不等同于卖家推理失败，应允许幂等重试，不能重复收费。

“故障不收费”指推理费。链上已发生的 Gas 不会因业务失败而自动退回。建议平台承担每次请求的锁款、结算交易 Gas；买家首次授权、存款和提款所需 Gas 在界面单独说明。

## 资金与信任边界

- 平台负责链外事实判定；首版应描述为“链上托管与结算、平台计量和判责”。
- 合约限制提款权限、单次预算、账户授权总额及期限；这些约束不等于合约能验证模型真实或 Token 数可信。
- 并发锁款同时占用账户可用余额与授权额度，不能让多个请求重复占用同一份资金。
- 撤销或过期授权阻止新请求；已锁定订单只允许在原预算和截止时间内完成结算。
- 每单设置结算截止时间。期限前允许结算；期限到达后允许买家直接调用合约释放资金，无需平台同意。
- 成功结算、失败释放、超时释放只能执行其中一次；已释放订单不能补扣。
- 可用余额可由买家直接提款。平台停机不能永久冻结资金。
- 单笔消费上限、授权总额和超时回收应由合约验证，不仅在网页中检查。
- Prompt、Response、文件、API Key 不进入交易 calldata、事件或合约存储。钱包、交易金额和结算时间等元数据仍可能公开。
- 首版不需要响应正文 Hash；请求 ID 和账单记录已足以支撑演示。

## 第二版方向

TEE 验证代码和执行环境；若要证明调用的上游模型，需要设计相应的连接验证、请求绑定和回执证据，不能把运行于 TEE 直接等同于真模型证明。

投诉裁决需要证据标准、裁决方、扣罚规则与申诉流程。模型评分和回答风格只作线索，不作为直接扣质押的唯一证据。首版不实现自动模型识别、自动罚没、完整订单簿、批量结算或真实模型接入。

## 验收场景

1. 两个独立卖家节点发布不同报价，买家能指定卖家或自动匹配。
2. 网页与 API 各完成一次请求，展示相同格式的账单与测试网结算记录。
3. 卖家在已输出部分内容后失败，整单收费为 0，全部锁款释放。
4. 预留 1.00、取消时实际费用 0.20，买家取消后收取 0.20、释放 0.80。
5. 达到预算上限正常停止，费用绝不超额，也不被当成卖家失败全免。
6. 缓存命中场景展示独立用量与费用，不重复计算输入。
7. 相同幂等 Key 重试不会创建第二次锁款或收费。
8. 两个并发请求不能超出余额或授权额度；超额请求被拒绝。
9. 平台停止处理后，超时订单仍可由买家直接释放并提款。
10. 所有演示明确标注模拟推理和测试资产，正文不上链。

## 交付顺序

先完成合约和 Router 的锁款、正常结算、失败释放主流程，再接独立卖家节点与最小 API；在线体验台共用这些接口。最后验证取消、预算、缓存、幂等与超时回收，并预留演示排练时间。

8 小时内优先保证端到端闭环与验收场景。默认提供中文界面与 curl 调用示例，卖家客户端采用可独立启动的进程和本地网页；桌面安装包、品牌包装和额外 SDK 适配不占用关键路径。

## 比赛与仓库约束

- 用户已明确部署到 Monad 测试网；没有新增待决产品问题。
- 用户希望体现 Web3 价值。演示重点为链上挂牌与报价、单次预算锁款、按实际用量结算、卖家故障全额释放，以及平台失联时买家自行回收超时资金。
- 仓库为 `onekb/Hackathon260905`。已在 `/Users/jacky/Documents/code/projects/260905` 初始化本地 `main` 分支，按用户要求设置 SSH 远程 `git@github.com:onekb/Hackathon260905.git`，并成功验证访问；远程目前为空，尚未提交或推送现有文件。
- 公网托管平台、公开发布仓库和录屏没有新增明确要求，不作为开发前置阻塞，也不据此擅自公开发布。

业务部署记录位于 `contracts/deployments/inferpool-monad-testnet.json`，实际验收证据见 `docs/`。Counter 的历史部署不作为本业务的完成证据。
